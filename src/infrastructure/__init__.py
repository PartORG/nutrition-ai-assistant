"""
infrastructure - Concrete implementations of domain ports.

Contains all vendor-specific code: Ollama, LangChain, FAISS, SQLite, CNN.
Depends on domain/ only (implements ports). Never imported by application/.
"""
